const router = require('express').Router()

/**
 * Call this route only when you want to use the Microsoft TTS API
 */
router.post('/', (req, res, next) => {
    // TODO - This route
    // TODO - Check if the speech is into the database
})

module.exports = router